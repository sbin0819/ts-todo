import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  background: coral;
  flex: 1 1 auto;
`;

const Todo = () => {
  return (
    <Container>
      <div>todo</div>
    </Container>
  );
};

export default Todo;
